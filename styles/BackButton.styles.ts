import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 8,
    position: 'absolute',
    top: 18,
    left: 12,
    zIndex: 10,
  },
});

export default styles;
